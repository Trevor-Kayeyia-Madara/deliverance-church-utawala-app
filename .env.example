# Local Postgres via docker-compose.yml
DATABASE_URL="postgresql://church:church_password@localhost:5432/church_app?schema=public"

# Optional (used when generating absolute URLs in server-side fetches)
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

