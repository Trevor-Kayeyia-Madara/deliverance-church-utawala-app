services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: church
      POSTGRES_PASSWORD: church_password
      POSTGRES_DB: church_app
    ports:
      - "5432:5432"
    volumes:
      - church_db_data:/var/lib/postgresql/data

volumes:
  church_db_data:

